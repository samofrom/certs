import React, { FC } from 'react';
import { Typography } from '@mui/material';

const Main: FC = () => {
  return (
    <Typography variant="h3" align="center">
      👋 Hi there!
    </Typography>
  );
};

export default Main;
